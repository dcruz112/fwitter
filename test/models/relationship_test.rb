require 'test_helper'

class RelationshipTest < ActiveSupport::TestCase
  # test that follow creates a relationship
  # test that a relationship includes follower/followed ids
  # not valid when relationship.followed_id = nil, same for follower
end
