<p id="notice"><%= notice %></p>

			<div class= "user-profile-info">
				<h1><%= @user.full_name %></h1>
				<h2><%= @user.handle_at %></h2>
				<h3><%= @user.biography if @user.biography %></h3>
				<h3><%= @user.current_location if @user.current_location %></h3>
				<br>

				<% if current_user == @user %>
				<%= link_to 'Edit Profile', edit_user_path(@user) %> 
				<% end %>
			</div>


<div>
	<%= render Tweet.where(poster_id: @user.id).reverse %>
</div>

<!-- this is where we include a loop to display those followed, or a link to the follower page -->

<!-- how do we get the user's followers to show up here? We probably add them all to an array? -->

<div class="user-sidebar">

<div>
	<%= render 'follow_form' %>
</div>

<div> 
	<%= render 'stats' %>
</div>


<%= link_to 'Accounts', users_path %> |
