<% if @total_stream.any? %>
	<% @total_stream = @total_stream.sort_by {:created_at} %>
	<ul class="tweet-stream">
		<% @total_stream.reverse.each do |post| %>
			<%= render post %>
		<% end %>
	</ul>
<% end %>
