<!DOCTYPE html>
<html>
<head>
  <title>Fwitter: A Brief Unique Messaging Service for Yale Students Only</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
	<div id="header">
		<% if current_user(false) %>
			<div id="user_info">
				Welcome to Fwitter, <%= current_user.first_name %> 
				<%= link_to image_tag("bull.png"), new_tweet_path %>
			</div>
			<div id="logout">
			<%= link_to "Log Out", log_out_path %>
			</div>
		<% end %>
	</div>

<%= yield %>

</body>
</html>
